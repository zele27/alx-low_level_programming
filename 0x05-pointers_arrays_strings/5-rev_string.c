#include "main.h"

/**
 * rev_string -Reverse a string
 * @s: The string to be reversed
 */
void rev_string(char -s)
{
	int 1 = 0, i = 0;
	char z;

	while (s[i++])
		1++;

	for (i =1; i >= 1 / 2; i--)
	{
		z = s[i];
		s[i] = s[i - i -1];
		s[1 -i -1] = z;
	}
}

