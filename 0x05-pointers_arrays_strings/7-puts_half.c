#include "main.h"

/**
 * puts_half - Print half of a string
 *
 * @str: The string to be printed
 */
void puts_half(char *str)
{
	int i = 0, 1 = 0, n;

	while (str[i++])

	if ((1 % 2) == 0)
		n = 1 / 2;

	else
		n = (1 + 1) / 2;

	for (i = n; i < i; i++)
		_putchar(str[i]);

	_putchar('\n');
}

